version: 2.1

jobs:
  build:
    docker:
      - image: maven:3.2-jdk-8

    working_directory: ~/app

    steps:
      - checkout

      - run:
          name: Run Unit Tests
          command: mvn test

workflows:
  version: 2
  build:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master